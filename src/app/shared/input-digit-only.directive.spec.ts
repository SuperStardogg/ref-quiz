import { InputDigitOnlyDirective } from './input-digit-only.directive';

describe('InputDigitOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDigitOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
